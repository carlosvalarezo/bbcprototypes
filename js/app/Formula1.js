var React = require ( 'react' );
var ReactDOM = require('react-dom');

var Formula1 = React.createClass({
    render:function()
    {
        return <div>Formula1</div>
    }

});
export default Formula1;
