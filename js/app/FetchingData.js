/*Component to fetch data to every page*/
var React = require('react');
var whatwg = require('whatwg-fetch');

import App from './app';

var FetchingData = React.createClass({

    render:function(){
        console.log("EN FETCHINGDATA2");
        return (
            null
        );
    }
});

export default FetchingData;
